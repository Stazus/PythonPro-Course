side_A_length = float(input('Type side "A" length: ')) # proszę o podanie długości boku "A", który jest liczbą zmiennoprzecinkową
side_B_length = float(input('Type side "B" length: ')) # prosze o podanie długości boku "B", który jest liczbą zmiennoprzecinkową
area = side_A_length * side_B_length # pole (obszar) prostokąta bok "A" * bok "B"

print(f'area equals: {area}')