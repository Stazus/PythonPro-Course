name: Lekcja 38 - Zadanie 16

on:
  push:

jobs:
  secrets-management:
    runs-on: ubuntu-latest

    steps:
      - name: Use secret
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          echo "Masked secret: $API_KEY"

      - name: Simulate deploy with secret
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          echo "Deploying application..."
          echo "Using API key: $API_KEY"
          echo "Deploy completed!"
